<% if @removed %>
	var href = '/tags/<%= @id %>';
	// remove the row
	$('table.tag-list tr').find('a[href="' + href + '"]').parents("table.tag-list tr").hide();
<% else %>
	alert('<%= @tag_message %>');
<% end %>
